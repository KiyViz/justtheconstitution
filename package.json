{
  "name": "justtheconstitution",
  "version": "2.0.0",
  "private": true,
  "description": "The U.S. Constitution — full text, no ads, no commentary.",
  "scripts": {
    "build": "node build.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
